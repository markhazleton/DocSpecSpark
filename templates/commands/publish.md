# /docspark.publish

DocSpark is a document AI tool derived from DevSpark.

Publish the approved markdown corpus as a GitHub Pages-ready static site. Create a docs landing page, organize navigation intentionally, link every publishable document, and prefer deterministic markdown-first tooling such as MkDocs for generation and deployment.