# /docspark.upgrade

DocSpark is a document AI tool derived from DevSpark.

Upgrade the installed DocSpark stock assets in a target repository without overwriting `.documentation/` customizations or personal overrides.