# /docspark.personalize

DocSpark is a document AI tool derived from DevSpark.

Create a personal command override under `.documentation/{git-user}/commands/` while preserving the team baseline and stock DocSpark defaults.