# Installation

Install this document AI workflow via CLI:

```bash
uv tool install docspark-cli --from git+https://github.com/markhazleton/docspark.git
docspark init my-docs-repo
```

Or point an AI assistant at a prompt under `quickstart/` to install the DocSpark workflow derived from DevSpark.