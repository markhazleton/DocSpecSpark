# Upgrading

DocSpark upgrades should replace `.docspark/` assets while leaving `.documentation/` untouched, preserving the document AI customization layer derived from DevSpark.

Review custom overrides after every upgrade to decide whether team changes should stay local or move back into the stock set.